import java.util.*;

/* strategy
    send a pointer k elements ahead using: prioritising right then left
    both pointers traverse, priotising right then left until fast pointer reachess null node

*/
class Problem15_3 {
    public static void main(String[] args) {
       
    }


    public static List<Integer> findKthLargestElements(TreeNode root, int k) {
        // send fast pointer k ahead
        TreeNode fast = root;

        // move both pointers in tandem until fast hits null

        // move slow pointer, adding nodes to array

        return new ArrayList<Integer>(Arrays.asList(0)); 
    }
    
    public static TreeNode search(TreeNode n) {
        


        return new TreeNode(0, null, null);
    }
}
